import { SoundOutlined } from '@ant-design/icons';

const AudioFile = ({ fileName }: { fileName: string }) => {
  return <SoundOutlined className="w-[100%] h-[150px]" />;
};

export default AudioFile;
