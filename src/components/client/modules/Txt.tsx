import { FileTextOutlined } from '@ant-design/icons';

const TextFile = ({ fileName }: { fileName: string }) => {
  return <FileTextOutlined className="w-[100%] h-[150px]" />;
};

export default TextFile;
