import { FormEnter } from '@/components/client';

const AuthorizationPage = () => {
  return (
    <div className="flex min-h-screen flex-col items-center justify-between p-24">
      <FormEnter />
    </div>
  );
};

export default AuthorizationPage;
